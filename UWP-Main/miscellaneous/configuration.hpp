#pragma once
#include <Windows.h>
#include <string>

namespace cherry {
	namespace configuration {
		/* regular */
		const std::string exploitName = "Cherry";
		const std::string exploitVersion = "1.0.0";

		/* pipe settings */
		const std::string pipeName = "Cherry";
		const bool usePipePid = false;
	};
};